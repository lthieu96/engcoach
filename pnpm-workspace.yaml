# Single-package repo. `packages` is kept so any pnpm version treats this as a
# valid (one-package) config. These native deps are transitive and unused at
# runtime (no next/image; Kokoro's onnx runs in-browser), so skip their builds.
packages:
  - .
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
