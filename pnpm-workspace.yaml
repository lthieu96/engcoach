# Single-package repo. The `packages` field is required by pnpm 10.11.x when a
# workspace file exists at all — older versions error "packages field missing or
# empty" on a settings-only file (the deploy failure this fixed).
packages:
  - .
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
